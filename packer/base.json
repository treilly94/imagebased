{
  "variables": {
    "do_token": ""
  },

  "builders": [
    {
      "type": "digitalocean",
      "api_token": "{{user `do_token`}}",
      "image": "ubuntu-16-04-x64",
      "region": "lon1",
      "size": "512mb",
      "ssh_username": "root",
      "snapshot_name": "base-{{isotime \"2006-01-02\"}}"
    }
  ],

  "provisioners": [
    {
      "type": "shell",
      "inline": "touch /test.txt"
    }
  ],

  "sensitive-variables": ["do_token"]
}